Directory:
setenv.sh allows a tomcat startup to occur that designates a password for the graph-resolve service.  
For tomcat 8 it needs to live in the <CATALINA Root Folder>/libexec/bin folder and be run in 
 conjunction with catalina.sh:  ./catalina.sh start